#pragma once
#include "Header\EngineCore.h"

class Scene
{
public:

protected:

private:

public:
	bool Init();
	void OnEnterScene();
	void OnExitScene();

	void Update(float DeltaTime);
	void LateUpdate(float DeltaTime);
	void Render();

protected:

private:

};

